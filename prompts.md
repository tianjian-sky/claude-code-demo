```
创建一个基于最新版本react和最佳的react生态系统的示例项目，需要支持ts和tsx，构建工具使用webpack5,css框架使用tailwind
  css，项目里同时里面需要包含一个模块展示gltf格式三维模型预览功能,基于three.js
```

```
哪个命令可以将我输入的内容加入CLAUDE.md
```

```
为了代码文件格式更加规范，请引入eslint及prettier
```

```
![alt text](image.png)
这部分有点丑，能不能美化一下使他们更美观并且适当增加动画效果
```

```
@src/components/ModelViewer.tsx 为这个文件里的加载模型方法load添加一些异常处理代码，需要对这些异常情况做处理：1.模型文件下载超时 2.模型下载地址不存在 3.模型文件格式不匹配导致load过程中出现异常
```

```
@src/components/ModelViewer.tsx 为这个组件的模型加载方法中，增加obj格式模型文件的加载代码。可以适当的重构原有的代码逻辑，使他未来能更方便地支持更多格式的模型文件加载
```

```
开发一个页面组件，实现https://www.figma.com/design/ibkLY9wsmpuGVBxf2RFmaf/532861047-s-team-library?node-id=0-1&p=f&t=hNyitbCFR3AyIo9M-0 这个网页里的第9张设计稿
```

```
好的，那就按照这个计划执行
```
